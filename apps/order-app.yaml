# Order cluster: emailservice + paymentservice + shippingservice + adservice
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orderservice
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: online-boutique
spec:
  project: default
  source:
    repoURL: https://github.com/ashikMostofaTonmoy/app-of-apps-demo.git
    targetRevision: main
    path: charts/orderservice
  destination:
    server: https://kubernetes.default.svc
    namespace: onlineboutique
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
