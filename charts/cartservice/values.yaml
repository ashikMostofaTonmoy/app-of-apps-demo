# Mirrors official helm-chart/values.yaml image structure
images:
  repository: us-central1-docker.pkg.dev/google-samples/microservices-demo
  tag: "v0.10.5"

cartservice:
  replicaCount: 1

# redis-cart uses public Docker Hub image (same as official chart publicRepository: true)
redis:
  image: redis:alpine
  replicaCount: 1
